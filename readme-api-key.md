# 🔑 Настройка командного API ключа

## Вариант 1: Файл api-key.txt (РЕКОМЕНДУЕТСЯ)

1. Создай файл `api-key.txt` в корне репозитория
2. Вставь свой API ключ (одной строкой):
   ```
   AIzaSyYOUR_ACTUAL_API_KEY_HERE
   ```
3. Загрузи на GitHub
4. Команда сразу может пользоваться!

**Когда научишь команду создавать ключи:**
- Просто удали файл `api-key.txt` из репозитория
- Приложение автоматически начнёт запрашивать личные ключи

---

## Вариант 2: Файл config.js (альтернатива)

1. Создай файл `config.js` в корне репозитория
2. Вставь код:
   ```javascript
   window.TEAM_API_KEY = 'AIzaSyYOUR_ACTUAL_API_KEY_HERE';
   ```
3. Загрузи на GitHub

---

## Как получить API ключ:

1. Открой: https://aistudio.google.com/apikey
2. Войди через Google аккаунт
3. Нажми "Create API key"
4. Скопируй ключ (начинается с `AIzaSy...`)
5. Вставь в `api-key.txt` или `config.js`

---

## Приоритет загрузки ключей:

1. **api-key.txt** (если есть)
2. **config.js** (если api-key.txt нет)
3. **Личный ключ** (если файлов нет → запрос у пользователя)

---

## Безопасность:

⚠️ **НЕ коммить ключ в публичный репозиторий!**

Если репозиторий публичный:
- Добавь в `.gitignore`:
  ```
  api-key.txt
  config.js
  ```
- Или используй GitHub Secrets (для приватных репозиториев)

---

## Структура файлов:

```
📁 mytlgapp/
├── index.html (основное приложение)
├── api-key.txt (твой командный ключ) ← СОЗДАЙ ЭТОТ ФАЙЛ
└── README.md (эта инструкция)
```

или

```
📁 mytlgapp/
├── index.html (основное приложение)
├── config.js (твой командный ключ) ← СОЗДАЙ ЭТОТ ФАЙЛ
└── README.md (эта инструкция)
```
